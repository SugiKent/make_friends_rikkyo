<%= form_for @department, :url => { :controller => "admin/departments", :action => :create } do |f| %>
  <% if @department.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@department.errors.count, "error") %> prohibited this admin_department from being saved:</h2>

      <ul>
      <% @department.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.collection_select(
          :faculty_id,
          Faculty.all,
          :id,
          :faculty_name,
          { include_blank: "Please select one" },
          { class: "form-control" }
    ) %>
  </div>

  <div class="form-group">
    <label>学科名</label>
    <%= f.text_field :department_name, class: "form-control" %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
