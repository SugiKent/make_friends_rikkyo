<div class="Form">
  <div class="Form__group">
    <label class="Form__label">Title</label>
    <%= f.text_field :title, :placeholder => "スレッドのタイトル", class: "Form__text-box Form__item" %>
  </div>

  <div class="Form__group">
    <label class="Form__label Form__label--text-area">Description</label>
    <%= f.text_area :description, :placeholder => "このスレッドはテニスサークルに入りたいひとで気軽に話し合うスレッドです！", rows: 50, class: "Form__text-box Form__item Form__item--text-area" %>
  </div>

  <div class="Form__line">
    <p>
      スレッドの内容に関連するサークルを選んでください。
      <br>サークルに興味のある人が集まりやすくなります。
    </p>
  </div>


  <div class="Form__group">
    <label class="Form__label Form__label--wide">関連するサークル</label>
    <div class="Check">
      <p class="Check__label">
        Sport
      </p>
      <div class="Check__group">
        <%= f.collection_select(
              :belong_id,
              Circle.where(category: 0),
              :id,
              :name,
              { include_blank: "No Selected" },
              { class: "Form__select-box Form__item for_hidden" }
        ) %>
      </div>
    </div>
    <div class="Check">
      <p class="Check__label">
        Culture
      </p>
      <div class="Check__group">
        <%= f.collection_select(
              :belong_id,
              Circle.where(category: 1),
              :id,
              :name,
              { include_blank: "No Selected" },
              { class: "Form__select-box Form__item for_hidden" }
        ) %>
      </div>
    </div>
  </div>

  <div class="Form__submit">
    <%= f.submit "CREATE", class: "Button Button__submit", data: { disable_with: 'Connecting...' } %>
  </div>
</div>
